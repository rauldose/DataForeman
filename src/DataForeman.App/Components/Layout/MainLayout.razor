@inherits LayoutComponentBase

<div class="page">
    <nav class="sidebar">
        <div class="sidebar-header">
            <span class="logo">⚙️</span>
            <span class="brand">DataForeman</span>
        </div>
        <div class="nav-menu">
            <NavLink class="nav-item" href="" Match="NavLinkMatch.All">
                <span class="nav-icon">🏠</span>
                <span class="nav-text">Home</span>
            </NavLink>
            <NavLink class="nav-item" href="connectivity">
                <span class="nav-icon">🔌</span>
                <span class="nav-text">Connectivity</span>
            </NavLink>
            <NavLink class="nav-item" href="charts">
                <span class="nav-icon">📊</span>
                <span class="nav-text">Charts</span>
            </NavLink>
            <NavLink class="nav-item" href="flows">
                <span class="nav-icon">🔀</span>
                <span class="nav-text">Flows</span>
            </NavLink>
        </div>
        <div class="sidebar-footer">
            <div class="engine-status">
                <span class="status-dot @(EngineConnected ? "connected" : "disconnected")"></span>
                <span>Engine: @(EngineConnected ? "Connected" : "Disconnected")</span>
            </div>
        </div>
    </nav>
    <main class="main-content">
        @Body
    </main>
</div>

@code {
    [Inject] private Services.RealtimeDataService? RealtimeData { get; set; }
    
    private bool EngineConnected => RealtimeData?.GetEngineStatus()?.IsRunning ?? false;
    
    protected override void OnInitialized()
    {
        if (RealtimeData != null)
        {
            RealtimeData.OnEngineStatusChanged += StateHasChanged;
        }
    }
}
